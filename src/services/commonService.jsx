import React from 'react';
/*
export const precioNormalizado = (number) => {
    return new Intl.NumberFormat("es-AR", {
        style: "currency",
        currency: "ARS",
        minimumFractionDigits: 0,
        maximumFractionDigits: 0
      }).format(number);
  };

module.exports = {
    precioNormalizado: precioNormalizado
};
*/